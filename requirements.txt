# 深圳亲子活动日历 - 数据质量巡检智能体
# Python 依赖（纯标准库 + requests 可选增强）

# 核心依赖（标准库即可运行，无需额外安装）
# urllib, json, datetime, hashlib, re, collections 均为 Python 标准库

# 可选增强依赖
requests>=2.28.0  # 可选：更友好的HTTP请求（标准库 urllib 已满足基本需求）